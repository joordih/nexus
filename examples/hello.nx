import std.io
import std.json
import std.fs.path
import std.network.http_client

main(): Void {
    io.println("Hola, Nexus!")
    var v = json.parse("{\"msg\":\"hola\"}")
    io.println(json.stringify(v))
}